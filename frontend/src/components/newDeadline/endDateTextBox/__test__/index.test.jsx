import React from 'react'
import { render } from '@testing-library/react'

import EndDateTextBox from '../'

test('<EndDateTextBox /> renders without crashing', () => {
	render(<EndDateTextBox />)
})
